services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-resu}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-resu}",
        ]
      interval: 10s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3100
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/${POSTGRES_DB:-resu}
      TOKEN_AUTH: ${TOKEN_AUTH:-changeme}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:3000}
      GAME_DATA_ADMIN_EMAIL: ${GAME_DATA_ADMIN_EMAIL:-}
      GAME_DATA_ADMIN_ACCOUNT_ID: ${GAME_DATA_ADMIN_ACCOUNT_ID:-}
      GAME_DATA_ADMIN_PROXY_TOKEN: ${GAME_DATA_ADMIN_PROXY_TOKEN}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:3100:3100"

volumes:
  postgres_data:
